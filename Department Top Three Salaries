class Solution:
    def findRepeatedDnaSequences(self, s: str) -> List[str]:
        _set = set()
        res = set()
        for i in range(9, len(s)):
            sub = s[i-9:i+1]
            if sub in _set:
                res.add(sub)
            _set.add(sub)
        return list(res)    
